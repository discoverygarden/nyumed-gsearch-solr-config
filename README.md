Please note that the credentials in conf/data-config.xml relate only to our internal Vagrant and not to their production. Any changes that take place will require the user to replace these credentials.

